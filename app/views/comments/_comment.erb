 <p>
  <span class='burn_comment_description'><%= comment.description %></span>
  <span class='burn_username'><%= current_user.username %></span>
  <span class='timestamp'><%= comment.created_at %></span>
</p>
